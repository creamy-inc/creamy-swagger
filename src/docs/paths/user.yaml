get:
  security:
    - Bearer: []
  tags:
    - users
  summary: "ユーザー情報取得"
  description: "ログインユーザーの情報を取得します"
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AppUser'

get:
  security:
    - Bearer: []
  tags:
    - room
  summary: "参加可能なルーム一覧を取得"
  description: "ユーザーが参加可能なルーム一覧を取得する"
  responses:
    200:
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/ParticipateRoom"
    400:
      $ref: "#/components/responses/400"
    401:
      $ref: "#/components/responses/401"
    500:
      $ref: "#/components/responses/500"

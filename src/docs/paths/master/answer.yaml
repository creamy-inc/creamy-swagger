get:
  tags:
    - master
  summary: "回答一覧を取得"
  description: "スケジュールアンケートの回答一覧を取得します"
  responses:
    200:
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Status"
    400:
      $ref: "#/components/responses/400"
    500:
      $ref: "#/components/responses/500"

get:
  tags:
    - master
  summary: "レビュー一覧を取得"
  description: "レビューの回答一覧を取得します"
  responses:
    200:
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Review"
    400:
      $ref: "#/components/responses/400"
    500:
      $ref: "#/components/responses/500"

get:
  tags:
    - master
  summary: "wish一覧を取得"
  description: "wish一覧を取得します"
  responses:
    200:
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Wish"
    400:
      $ref: "#/components/responses/400"
    500:
      $ref: "#/components/responses/500"
